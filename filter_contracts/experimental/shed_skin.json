{
  "schema_version": "1.0",
  "contract_version": "1.0.0",
  "filter_id": "experimental.shed_skin",
  "filter_name": "Shed Skin",
  "family_id": "experimental",
  "filter_version": "1.0.0",
  "status": "accepted",
  "implementation_class": "D",
  "creative_proposition": "The film leaves voices behind.",
  "relationship_domains": {
    "reads": [
      "dialogue",
      "performance",
      "identity",
      "time"
    ],
    "changes": [
      "identity",
      "time"
    ],
    "preserves": {
      "progression": "Identity stage never decreases",
      "stage_identity": "One stable source identity per stage"
    }
  },
  "specimen_inputs": [
    "film"
  ],
  "required_analysis": [
    "dialogue_events",
    "performances",
    "speakers"
  ],
  "candidate_generation": "The output is divided into stages, each assigned one stable source speaker identity. Candidate pools exclude unusable zero-duration dialogue.",
  "selection_rules": [
    "Dialogue identity changes in ordered temporal stages and never reverts.",
    "Selection is deterministic for identical inputs, parameters, and random seed."
  ],
  "transformation_behavior": "The output is divided into stages, each assigned one stable source speaker identity.",
  "progression": "The emitted schedule measures the defining law at every selected output position.",
  "state_variables": [
    "parameter:identity_stages",
    "parameter:intensity"
  ],
  "fields": [
    "filter_metrics",
    "filter_validation",
    "mappings"
  ],
  "controlled_uncertainty": "All tie breaking is deterministic; the recipe records the random seed even when a law needs no random choice.",
  "hard_invariants": [
    {
      "id": "identity_stages_never_revert",
      "statement": "Identity stage never decreases through the output.",
      "validator": "filter_validation.identity_stages_never_revert"
    },
    {
      "id": "each_stage_has_one_stable_identity",
      "statement": "Each stage uses one stable source identity.",
      "validator": "filter_validation.each_stage_has_one_stable_identity"
    }
  ],
  "validation": {
    "strategy": "Validate strategy-emitted invariants, measurable coverage, silence ratio, provenance, and the final audio stream.",
    "required_metrics": [
      "replacement_count",
      "silence_ratio"
    ]
  },
  "failure_behavior": "Fail before rendering when required analysis, candidate evidence, or the defining invariant is unavailable.",
  "preview_strategy": "Choose representative mappings that expose the defining law and its emitted evidence.",
  "renderer_requirements": [
    "Preserve the source picture unless the contract explicitly says otherwise.",
    "Produce a final MP4 with a verified audio stream.",
    "Report replacement-audio provenance, dialogue coverage, silence ratio, and invariant results."
  ],
  "procedure_behavior": {
    "support_status": "single_step_validated",
    "receives_transformed_specimen": false,
    "commitment": "When a Procedure runtime exists, consume prior mappings without losing immutable source provenance; this phase does not execute that commitment."
  },
  "capabilities": {
    "implemented": true,
    "implementation_key": "shed_skin",
    "execution_mode": "scheduling_strategy",
    "sparse_schedule": true,
    "requires_speaker_identity": true,
    "supports_preview": true,
    "supports_full_length": true,
    "requires_output_acceptance": true
  },
  "blockers": []
}
